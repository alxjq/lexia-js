# Lexia CLI Assistant (Node.js)

Lexia is a password-protected terminal-based AI assistant built with Node.js.  
It connects to the Groq API using an OpenAI-compatible endpoint and allows interactive conversations directly from the command line.

---

## 🚀 Features

- 🔐 Password-protected access
- 💬 Interactive CLI chat interface
- 🤖 Custom AI personality (system prompt)
- 🌐 Uses Groq API (OpenAI-compatible endpoint)
- 🧵 Asynchronous HTTPS requests
- ❌ Exit command support

---

## 📦 Requirements

- Node.js v16+ (recommended v18+)
- Internet connection (for API requests)
- A valid Groq API key

---

## ⚙️ Installation

1. Clone the repository:

```bash
git clone https://github.com/yourusername/lexia-cli.git
cd lexia-cli
